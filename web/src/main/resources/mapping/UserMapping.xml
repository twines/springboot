<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.twins.lee.mapper.UserMapper">
    <resultMap id="BaseResultMap" type="com.twins.lee.entity.User">
        <result column="id" jdbcType="INTEGER" property="id"/>
        <result column="user_name" jdbcType="VARCHAR" property="userName"/>
        <result column="password" jdbcType="VARCHAR" property="password"/>
        <result column="real_name" jdbcType="VARCHAR" property="realName"/>
    </resultMap>

    <select id="getUserById" resultType="com.twins.lee.entity.User">
        select * from user where id = #{id}
    </select>

    <select id="selectUserByPage" resultType="com.twins.lee.entity.User">
        SELECT * from user where id > #{id}
    </select>
</mapper>