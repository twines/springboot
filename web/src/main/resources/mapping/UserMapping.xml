<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.twins.lee.mapper.UserMapper">

    <resultMap id="userResultMap" type="com.twins.lee.entity.User">
        <result column="id" jdbcType="INTEGER" property="id"/>
        <result column="user_name" jdbcType="VARCHAR" property="userName"/>
        <result column="password" jdbcType="VARCHAR" property="password"/>
        <result column="real_name" jdbcType="VARCHAR" property="realName"/>
        <collection property="roles" ofType="com.twins.lee.entity.Role">
            <id column="role_id" property="id"/>
            <result column="role" jdbcType="VARCHAR" property="role"/>
            <result column="description" jdbcType="VARCHAR" property="description"/>
            <result column="available" jdbcType="INTEGER" property="available"/>
        </collection>
    </resultMap>

    <select id="getUserById" resultMap="userResultMap">
    SELECT   u_r.id, u_r.user_name,u_r.password, u_r.real_name, r.id role_id, r.role, r.description, r.available  FROM role r LEFT JOIN (SELECT u.*, ur.role_id FROM user u , user_role ur where  u.id=#{id}) u_r  ON u_r.role_id = r.id
</select>
<select id="getUserByName" parameterType="java.lang.String">
        SELECT   u_r.id, u_r.user_name, u_r.real_name, r.id role_id, r.role, r.description, r.available  FROM role r LEFT JOIN (SELECT u.*, ur.role_id FROM user u , user_role ur where  u.user_name=#{userName}) u_r  ON u_r.role_id = r.id
</select>
    <select id="selectUserByPage" resultType="com.twins.lee.entity.User">
        SELECT * from user where id > #{id}
    </select>
</mapper>